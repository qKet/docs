# Q-ket 3차 최종발표 대본

`Q-ket 3차_최종발표_v2.pptx` 스피커 노트를 페이지별로 옮긴 것. 슬라이드를 고치면 이 파일도 다시 내보내야 함(반대 방향 동기화 아님 — pptx가 원본).

- 총 발표 시간: 25분 (영상 9분 포함 → 말할 시간 16분)
- 대본 분량(38·39번 제외): 약 3,543자 → 300자/분 기준 약 11.8분, 250자/분 기준 약 14.2분
- 🔻 표시는 시간이 더 필요하면 삭제/축약 후보로 논의된 슬라이드([27]번 CPU request 미스매치와 내용이 겹치는 [32]번, 스크린샷만 있는 [20]번)

## 01. 프로젝트 개요 (1~9번)

**1.** 안녕하십니까. 5팀 발표를 시작하겠습니다.
저희가 만든 서비스는 공연 티켓 예매 플랫폼 Q-ket 입니다.

**2.** 발표는 총 6가지 목차를 순서대로 진행될 예정입니다.

**3.** 첫번째 챕터는 프로젝트 개요 입니다.

**4. 주제 선정 배경**
공연 티켓 오픈은 순간적으로 트래픽이 집중되는 상황입니다. 저희는 여기서 세 가지 문제를 봤습니다 — 동시 접속 폭주, 실패 시 무한 재시도로 인한 트래픽 증폭, 그리고 중복 예매입니다. 이 세 가지를 인프라와 애플리케이션을 함께 설계해서 풀어야 한다고 판단해 이 주제를 선택했습니다.

**5. 2차 → 3차 목표**
2차 프로젝트에서 대기열과 동시성 제어를 설계·구현하고 EKS에 배포까지 마쳤습니다. 3차 목표는 이게 실제로 얼마나 버티는지 확인하는 것이었습니다. 목표는 동시 접속 1만 명, 실측 검증은 4천 명, 대기열 동시 입장은 400명 기준으로 설계했습니다.

**6. 3차 신규 기능**
3차에서 네 가지를 새로 붙였습니다. 토스페이먼츠 결제와 소셜 로그인, 예매자만 쓸 수 있고 OpenAI로 스포일러를 가리는 감상평, SQS·Lambda·SES 기반 알림, 그리고 보안 강화입니다. 보안을 보강하는 과정에서 실제 취약점도 하나 발견했는데, 이건 챕터 5에서 말씀드리겠습니다.

**7. 기술 스택**
다음은 계층별 기술 스택입니다. 파란 뱃지가 3차에서 새로 도입한 것들인데, 대부분 인프라·CI/CD·모니터링 등 운영 쪽입니다.

**8. 팀 구성**
팀 구성입니다. 6개 파트를 다섯 명이 나눠 맡았는데, 모든 영역에 최소 두 명씩 배치해서 한 사람이 빠져도 진행이 멈추지 않게 했습니다.

**9. 진행 일정**
진행 일정은 각 주차별로 기획 설계, 기능개발, 인프라 고도화, 분석테스트, 발표 순으로 진행을 하였습니다.

## 02. 수행절차 및 방법 (10~13번)

**10.** 다음으로는 수행절차 및 방법 입니다.

**11. 수행절차·협업 방식**
수행절차는 사전기획 1주, 수행 2~3주, 검증 순으로 진행했고, Git Flow로 feature→dev→release(검증)→main(배포) 단계를 거쳤습니다. 모든 변경은 PR 리뷰 후 병합했고, 장애와 의사결정은 그때그때 문서로 남겼는데 이게 나중에 크게 도움이 됐습니다.

**12. 단계별 요약**
사전기획에서 성능 목표를 먼저 잡고, 기능을 빠르게 완성해 실제 흐름 기반 부하 시나리오를 확보했습니다. 검증 단계에서 병목을 특정하고 개선안을 도출했습니다.

**13. 여섯 갈림길(기술 선택)**
여섯 가지 주요 결정입니다. 대기열은 순번 조회가 빨라야 해서 Redis Sorted Set을, 동시성은 파드가 여러 대라 분산 락을, 알림은 SQS를 썼습니다. 파드 스케일링은 나중에 대기열 길이 트리거로 바꿀 수 있게 KEDA를 썼고, 배포는 현업 멘토 조언대로 자동 대신 사람이 시점을 정하는 방식으로 했습니다.

## 03. 수행경과 (14~22번)

**14.** 다음은 수행경과 입니다.

**15. 예매 흐름**
예매 흐름입니다. 접속하면 대기열 토큰을 받고 3초마다 순번을 확인합니다. 입장하면 좌석 선택 시 분산 락이 걸리고, 결제가 끝나면 알림이 발송됩니다. 대기열 토큰 없인 예매 서비스에 접근할 수 없고, 중복은 분산 락과 조건부 갱신 두 겹으로 막습니다. 이 흐름을 그대로 부하테스트 시나리오로 썼습니다.

**16. 인프라 구성(네트워크)**
2개 가용영역에 노드와 데이터베이스를 나눠 배치했습니다. 서브넷은 Public(NAT)·Private(워크로드)·Private(데이터) 3층이고, SSH는 아예 막고 SSM Session Manager로만 접근합니다.

**17. 클러스터 내부**
1번, ALB Controller와 ExternalDNS가 로드밸런서·도메인을 자동으로 만듭니다. 2번, External Secrets Operator가 Secrets Manager 값을 K8s Secret으로 만들어서 Git엔 비밀번호가 전혀 없습니다. 3번, NetworkPolicy로 네임스페이스별 통신을 격리합니다. 4번 오토스케일링은 다음 장에서 설명드립니다.

**18. 오토스케일링 동작방식**
CPU가 오르면 KEDA가 감지해 replica를 늘립니다. 자리가 없으면 여유 노드에 넣거나, 우선순위 낮은 파드를 밀어내거나, Karpenter가 새 노드를 만듭니다. 파드는 초 단위, 노드는 분 단위로 뜨는데 이 차이가 챕터 4에서 문제가 됩니다.

**19. CI/CD 및 배포 전략**
배포는 두 갈래입니다. 앱은 GitOps로 자동 — push하면 빌드·이미지 업로드·CD 반영까지 자동이고 ArgoCD Sync만 사람이 누릅니다. 인프라는 전부 수동이고, 매일 밤 컴퓨팅 레이어만 지워서 비용을 줄입니다.

**20. 🔻 CI 실제 화면**
빌드는 이미지 태그로 커밋 번호를 그대로 써서 운영 이미지에서 코드로 역추적이 됩니다. CI가 CD 저장소의 이미지 태그를 GitHub App 토큰으로 직접 커밋해서, 사람이 옮겨적는 단계도 개인 계정 의존도 없습니다.

**21. ArgoCD 실제 화면**
실제 배포 화면입니다. ArgoCD가 CD 레포 변경을 감지하면 메일로 알려주고, Sync 버튼만 누르면 배포됩니다.

**22. 보안 방식**
보안은 네트워크·권한·자격증명·애플리케이션 순으로 갖췄습니다. IRSA로 파드별 최소권한을 주고, ESO로 시크릿을 관리해 Git·CI에 저장된 키가 없습니다. 관측은 지표·로그·프론트 지표 세 각도로 봤고, 이 덕분에 다음 챕터에서 병목을 특정할 수 있었습니다.

## 04. 부하테스트 및 성능 개선 (23~28번)

**23.** 다음은 부하테스트 및 성능 개선 입니다.

**24. 시나리오**
규모별로 네 종류를 준비했고, 2천 명과 4천 명을 실제로 측정했습니다. 로그인부터 예매까지 전 구간을 3초 폴링까지 실제 화면 그대로 재현했고, 로그인 성공률·대기 시간·응답 지연·중복 예매를 측정했습니다.

**25. 2,000명 첫 측정**
2천 명 첫 측정에서 오토스케일링은 동작했지만 replica가 8까지 못 가고 4에서 멈췄고, 파드가 계속 재시작했습니다. 확인해보니 새 파드가 이미 CPU 꽉 찬 노드에 배치되고 있었습니다. 파드가 한 노드에 몰리는 게 문제라고 보고, 분산을 강제하면 해결될 거라 가설을 세웠습니다.

**26. 개선 시도와 역효과**
가설대로 분산 제약을 걸었습니다. 소프트 제약(ScheduleAnyway)은 효과가 없었고, 하드 제약(DoNotSchedule)으로 바꾸니 재시작은 0으로 줄었는데 로그인 성공률이 53%로 떨어졌습니다. 하드 제약이 새 노드 여러 대가 다 뜰 때까지 기다리게 만들어서, 그동안 기존 소수 파드가 트래픽을 전부 받았기 때문입니다.

**27. 병목 원인 분석**
왜 분산으로 안 풀렸는지 파고들었더니, 스케줄러는 파드가 0.25코어 쓴다고 계산했는데 실제로는 로그인의 BCrypt 연산 때문에 0.7~0.9코어를 썼습니다. 노드 하나에 파드 네 개면 이미 포화였던 겁니다. 그래서 요청량을 0.5코어로 현실화하니 노드당 수용 파드 수가 절반으로 줄어 분산이 자연히 강제됐고, 여기에 최소 파드 수 상향과 오버프로비저닝(풍선 파드로 노드 여유 선점)까지 더해서 하드 제약을 다시 살렸습니다.

**28. 개선 결과와 용량 산정**
결과는 대기 시간 641초→8.2초(78배), 전체 과정 11분→45초(15배), 실패율·재시작 전부 0입니다. 인원을 두 배(4천 명)로 올려도 같은 파드 8개로 처리했고 처리량은 오히려 늘었지만, 대기 시간만 45초로 늘었습니다. 즉 한계에 부딪히는 건 서버가 아니라 대기 시간이라는 뜻이고, 대기열이 없었다면 이 사람들은 기다린 게 아니라 실패했을 겁니다.

> (1만 명 확장 방법은 38번 Q&A 백업으로 이동 — 시간 남으면 요약만 추가 언급)

## 05. 트러블슈팅 (29~33번)

**29.** 다음은 트러블 슈팅입니다.

**30. 장애사례① — 증상/원인**
배포본에서 로그인 없이 관리자 API 전체에 접근 가능한 걸 발견했습니다. 세션 쿠키 없이 호출해도 200 OK로 실제 데이터가 나왔습니다. 원인은 경로를 가져오는 메서드 한 줄 — Context-Path까지 포함해서 반환하는 바람에 '/admin으로 시작하는지' 조건이 한 번도 참이 안 됐던 겁니다. 로컬 단위 테스트는 Context-Path가 없어서 통과했고, 배포 환경에서만 무력화됐습니다.

**31. 장애사례① — 조치/재발방지**
메서드를 교체해 Hotfix 배포하고 6개 API 전부 401·403 차단을 확인했습니다. 재발 방지로 세 가지를 했습니다 — 인증 없는 호출 테스트를 자동화하고, 권한 검사를 인터셉터 한 곳이 아니라 각 기능에도 이중으로 걸고, 로그인·권한 코드는 리뷰를 강화했습니다.

**32. 🔻 장애사례② — 부트스트랩 노드 과부하**
2천 명 테스트 중 고정 노드 CPU가 92%로 튀었습니다. 조사해보니 ArgoCD·모니터링 같은 인프라 파드 34개가 부트스트랩 노드 한 대에 몰려 있었고, 이 파드들은 자원 요청량이 비어 있어 노드가 부족해지면 가장 먼저 죽는 등급이었습니다. 요청량을 부여하고 노드를 2대로 늘려 분산했더니 CPU가 35~40%로 떨어졌습니다. Karpenter가 만드는 동적 노드만 보고 있었고, 고정 노드는 관측 밖이었다는 게 원인이었습니다.

**33. 멘토 피드백 및 개선 내역**
멘토 피드백 다섯 가지 중 넷은 반영했습니다. 코드·배포 레포 분리, OIDC 임시 인증으로 장기 키 제거, Ingress 대신 Gateway API 전환, Loki로 로그 통합 조회입니다. 이 Loki가 챕터 4에서 병목 찾을 때 실제로 쓰였습니다. prod를 별도 클러스터로 분리하라는 피드백은, 비용이 2배가 돼서 대신 네임스페이스+NetworkPolicy로 격리하는 걸로 판단했습니다.

## 06. 시연 및 자체 평가 (34~37번)

**34.** 다음은 시연 및 자체 평가입니다.

**35. 시연**
실제 동작 화면을 보시겠습니다. *(→ 9분 영상 재생)*

**36. 체크리스트**
체크리스트 15개 중 13개 완료입니다. 미완료 하나는 테스트 커버리지 부재로, 이번 인가 버그를 배포 후에야 발견한 원인이었습니다. 관측성은 부분 완료로 임계치 알림이 아직 없고, 부하테스트는 4천 명·중복 예매 0건으로 실측 완료했습니다.

**37. 잘한 점 / 아쉬운 점**
잘한 점 — 가설대로 했다가 로그인 53%로 떨어진 걸 실측으로 발견하고 되돌렸고, Terraform·GitOps로 인프라를 매일 재생성했고, 분산 락을 이중으로 설계해 중복예매 0건을 냈습니다. 아쉬운 점은 테스트 코드 부재, 알림 미구축, 1만 명 미검증인데 각각 대응 계획을 갖고 있습니다.

## 마무리 (38~39번)

**38. 클로징**
이상으로 5팀 발표를 마치겠습니다. 질문 받겠습니다. 감사합니다.

> 이 슬라이드 노트에 Q&A 백업 답변(부하테스트·용량산정 7문항 / 기술 선택 5문항 / 장애·보안 5문항 / 운영·프로세스 4문항 / 프로젝트 전반 3문항, 총 24문항)이 그대로 들어있음. 발표 중엔 안 읽고 질문 나올 때만 참고 — 원문은 pptx 38번 슬라이드 노트 참고.

**39. Thank you**
*(대본 없음 — 클로징 멘트는 38번에서 이미 함)*
